# Asset Descriptions

⚠️  GEMINI_API_KEY not set — descriptions below are catalog-derived (alt text, headings, section context, filename) instead of Vision-generated. To get richer Vision descriptions on the next capture, set GEMINI_API_KEY (or GOOGLE_API_KEY) and re-run.

The `logo-<hash>.svg` filename prefix is a structural hint (DOM said this SVG was inside a `<header>`, home-link `<a>`, or had an aria-label matching the page brand). To pick the actual brand logo without Vision, open the `logo-*` candidates in a previewer or rasterize them with `sharp` before referencing — composing a fake logo ships off-brand in the final video.

- svgs/logo-2409432b.svg — logo 2409432b
- svgs/logo-b7752207.svg — logo b7752207
- svgs/svg-1b0e8c1e.svg — svg 1b0e8c1e
- screens/workspace-editor.png — 1280×720 authenticated EdgeEver desktop workspace in light mode, showing the classic three-pane notebook/list/editor layout and a rich-text note with Markdown controls.
- screens/workspace-agent-mcp.png — 1280×720 authenticated workspace in light mode, showing the “Agent-ready: REST API 与 MCP” note, OpenAPI path, MCP endpoint, notebooks, tags, and note list.
- screens/workspace-agent-mcp-dark.png — 1280×720 authenticated workspace in dark mode, showing the same AI Agent, REST API, OpenAPI, and MCP workflow.
